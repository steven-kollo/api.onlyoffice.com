<script webc:setup>
/**
 * Resolve a tag to a template.
 * https://github.com/11ty/webc/issues/143
 *
 * @param {string} tag
 * @returns {string}
 */
function resolve(tag) {
  return `<template webc:is="${tag}"></template>`
}
</script>
<template webc:nokeep @html="resolve(tag)"></template>