// function makeDate(timestamp: number): Date
// function makeDate(m: number, d: number, y: number): Date
// function makeDate(mOrTimestamp: number, d?: number, y?: number): Date {
//   if (d !== undefined && y !== undefined) {
//     return new Date(y, mOrTimestamp, d);
//   } else {
//     return new Date(mOrTimestamp);
//   }
// }

// function b(): ReturnType<typeof makeDate> {
//   return ""
// }

// // if overload refer to the first declaration
// // in declaration should have see also
// // scheme should
// // function makeDate(timestamp: number): Date (+1 overload)


// if the hero title is long use the ellipsis
