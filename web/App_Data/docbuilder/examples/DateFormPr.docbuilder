// Create a date form with the common and specific date form properties:
var oDateFormPr = {"key": "Personal information", "tip": "Date of birth", "required": true, "placeholder": "Date of birth", "format": "mm.dd.yyyy", "lang": "en-US"};
var oDateForm = Api.CreateDateForm(oDateFormPr);