builder.CreateFile("xlsx");
oWorksheet = Api.GetActiveSheet();
themes = Api.GetThemesColors();
for (i = 0; i < themes.length; ++i) {
  oWorksheet.GetRange("A" + (i + 1)).SetValue(themes[i]);
}
builder.SaveFile("xlsx", "GetThemesColors.xlsx");
builder.CloseFile();